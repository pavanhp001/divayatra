'use strict';

angular.module('crudApp').factory('CustService',
    ['$localStorage', '$http', '$q', 'urls',
        function ($localStorage, $http, $q, urls) {

            var factory = {
               
            };
            return factory;
        }
    ]);